{
    "installCommand": "yarn global add vercel@latest && yarn && yarn testCi && yarn prisma generate",
    "buildCommand": "cd ../.. && npx turbo run build --filter=web... && env && sleep 10 && if [ \"$DEPLOY_STAGING\" = true ]; then vercel alias --token=$VERCEL_CLI_TOKEN $VERCEL_URL staging.jprojects.dev --scope=$VERCEL_SCOPE; fi"
}