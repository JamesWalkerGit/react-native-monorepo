{
    "installCommand": "yarn && yarn testCi && yarn prisma generate",
    "buildCommand": "cd ../.. && npx turbo run build --filter=web && npm install -G vercel && vercel alias $VERCEL_BRANCH_URL staging.jprojects.dev"
}