{
    "installCommand": "yarn global add vercel && yarn && yarn testCi && yarn prisma generate",
    "buildCommand": "cd ../.. && npx turbo run build --filter=web && vercel alias --token=$VERCEL_CLI_TOKEN $VERCEL_BRANCH_URL staging.jprojects.dev"
}