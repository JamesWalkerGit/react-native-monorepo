{
    "installCommand": "yarn && yarn testCi && yarn prisma generate && npm install -G vercel",
    "buildCommand": "cd ../.. && npx turbo run build --filter=web && vercel alias $VERCEL_BRANCH_URL staging.jprojects.dev"
}